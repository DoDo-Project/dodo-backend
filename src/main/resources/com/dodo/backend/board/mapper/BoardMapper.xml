<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dodo.backend.board.mapper.BoardMapper">

    <select id="findBoardList" resultType="com.dodo.backend.board.dto.response.BoardResponse$BoardListQueryResponse">
        SELECT
            b.board_id AS boardId,
            b.board_title AS boardTitle,
            b.board_content AS boardContent,
            (
                SELECT i.image_file_url
                FROM image_file i
                WHERE i.board_id = b.board_id
                ORDER BY i.image_file_id ASC
                LIMIT 1
            ) AS thumbnailImageUrl,
            u.nickname AS nickname,
            b.view_count AS viewCount,
            (
                SELECT COUNT(*)
                FROM comment c
                WHERE c.board_id = b.board_id
            ) AS commentCount,
            (
                SELECT COUNT(*)
                FROM reaction r
                WHERE r.board_id = b.board_id
                  AND r.reaction_type = 'LIKE'
            ) AS likeCount,
            (
                SELECT COUNT(*)
                FROM reaction r
                WHERE r.board_id = b.board_id
                  AND r.reaction_type = 'DISLIKE'
            ) AS dislikeCount,
            b.board_created_at AS createdAt,
            b.modified_at AS modifiedAt
        FROM board b
        INNER JOIN users u ON u.users_id = b.user_id
        WHERE b.board_status = 'PUBLISHED'
          AND b.board_type = 'FREE'
        ORDER BY b.board_created_at DESC, b.board_id DESC
        LIMIT #{size}
        OFFSET #{offset}
    </select>

    <select id="countPublishedBoards" resultType="long">
        SELECT COUNT(*)
        FROM board b
        WHERE b.board_status = 'PUBLISHED'
    </select>

    <select id="findMyBoardList" resultType="com.dodo.backend.board.dto.response.BoardResponse$BoardListQueryResponse">
        SELECT
            b.board_id AS boardId,
            b.board_title AS boardTitle,
            b.board_content AS boardContent,
            (
                SELECT i.image_file_url
                FROM image_file i
                WHERE i.board_id = b.board_id
                ORDER BY i.image_file_id ASC
                LIMIT 1
            ) AS thumbnailImageUrl,
            u.nickname AS nickname,
            b.view_count AS viewCount,
            (
                SELECT COUNT(*)
                FROM comment c
                WHERE c.board_id = b.board_id
            ) AS commentCount,
            (
                SELECT COUNT(*)
                FROM reaction r
                WHERE r.board_id = b.board_id
                  AND r.reaction_type = 'LIKE'
            ) AS likeCount,
            (
                SELECT COUNT(*)
                FROM reaction r
                WHERE r.board_id = b.board_id
                  AND r.reaction_type = 'DISLIKE'
            ) AS dislikeCount,
            b.board_created_at AS createdAt,
            b.modified_at AS modifiedAt
        FROM board b
        INNER JOIN users u ON u.users_id = b.user_id
        WHERE b.user_id = #{userId, typeHandler=com.dodo.backend.common.handler.UuidTypeHandler}
          AND b.board_status != 'DELETED'
        ORDER BY b.board_created_at DESC, b.board_id DESC
        LIMIT #{size}
        OFFSET #{offset}
    </select>

    <select id="countMyBoards" resultType="long">
        SELECT COUNT(*)
        FROM board b
        WHERE b.user_id = #{userId, typeHandler=com.dodo.backend.common.handler.UuidTypeHandler}
          AND b.board_status != 'DELETED'
    </select>

    <update id="updateBoard">
        UPDATE board
        <set>
            <if test="request.boardTitle != null and request.boardTitle != ''">board_title = #{request.boardTitle},</if>
            <if test="request.boardContent != null and request.boardContent != ''">board_content = #{request.boardContent},</if>
            modified_at = NOW()
        </set>
        WHERE board_id = #{boardId}
    </update>

    <update id="increaseViewCount">
        UPDATE board
        SET view_count = view_count + 1
        WHERE board_id = #{boardId}
    </update>

    <update id="deleteBoard">
        UPDATE board
        SET board_status = #{status},
            board_status_updated_at = NOW()
        WHERE board_id = #{boardId}
    </update>
</mapper>
